# =============================================================================
# VARIÁVEIS DE AMBIENTE - COTAÇÃO EXTERNA RENDER
# =============================================================================
# IMPORTANTE: Não commitar este arquivo com valores reais!
# Use variáveis de ambiente no Render Dashboard
# =============================================================================

# Chave secreta para sessões Flask
SECRET_KEY=gerar-chave-aleatoria-segura-aqui

# Chave de API compartilhada com sistema interno
# DEVE ser a mesma configurada no sistema principal
API_SECRET_KEY=chave-api-compartilhada-com-sistema-interno

# URL base da aplicação (preenchido automaticamente no Render)
BASE_URL=https://seu-app.onrender.com

# Origins permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:5001,http://127.0.0.1:5001

# Tempo de expiração do token em horas
TOKEN_EXPIRATION_HOURS=72

# Debug mode (false em produção)
FLASK_DEBUG=false

# Porta (definida automaticamente no Render)
PORT=5000
